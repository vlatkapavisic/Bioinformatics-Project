The program takes two files in an input and returns one. Input files can be FASTA files with a single sequence or simple text files. The output file is always in the FASTA format with lengths of sequences and time taken.
Nothing is returned to the console.
Called with:

perl SmithWaterman.pl <fileName1> <fileName2> <optional:FileOut>